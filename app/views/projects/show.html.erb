<h1><%= @project.name %></h1>

<%= link_to 'New Task', new_project_task_path(@project) %>
<br><br>

<% @project.tasks.each do |task| %>

  <ul>
    <li><%= link_to task.title, project_task_path(@project, task) %></li>
    <li>Due: <%= task.due %> </li>

  </ul>

<% end %>
